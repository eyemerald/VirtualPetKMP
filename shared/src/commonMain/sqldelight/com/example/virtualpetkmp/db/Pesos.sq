CREATE TABLE pesos (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    mascotaId INTEGER NOT NULL,
    fecha TEXT NOT NULL,
    peso REAL NOT NULL,
    notas TEXT,
    FOREIGN KEY (mascotaId) REFERENCES mascotas(id) ON DELETE CASCADE
);

selectByMascotaId:
SELECT * FROM pesos WHERE mascotaId = ? ORDER BY fecha ASC;

insert:
INSERT INTO pesos(mascotaId, fecha, peso, notas) VALUES (?, ?, ?, ?);

update:
UPDATE pesos SET fecha = ?, peso = ?, notas = ? WHERE id = ?;

delete:
DELETE FROM pesos WHERE id = ?;
